<?xml version="1.0" encoding="utf-8"?>
<!-- 類型 -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" android:layout_height="50dp" android:layout_width="50dp">
    <!-- 顏色 -->
    <solid android:color="#89afed" />
    <!-- 文字與邊框的間距 -->
    <padding
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp"
        android:left="10dp"
        />
    <!-- 設置圓角 -->
    <corners android:radius="15dp" />
    <!-- 邊框線 -->
    <stroke
        android:width="2dp"
        android:color="@android:color/black"
        android:dashGap="0dp"
        android:dashWidth="0dp" />
</shape>